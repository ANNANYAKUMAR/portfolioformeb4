class Powerup {
  //member variables
  int x, y, dia;
  char displayMode;
  color c;

  Powerup() {
    x=0;
    y=0;
    dia = 50;
    displayMode = '1';
    c= #FFE603;
  }
  //member methods
  void display() {
    fill(c);
    ellipse(x, y, dia, dia);
  }
}
